name: Release Datapack

on:
  push:
    branches:
      - main
    paths:
      - 'new_versions/**/datapack/**'
  workflow_dispatch:

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Find latest version
        id: version
        run: |
          latest=$(ls -1 new_versions/ | sort -V | tail -n 1)
          
          if [ -z "$latest" ]; then
            echo "ERROR: No version folders found in new_versions/"
            exit 1
          fi
          
          echo "mc_version=$latest" >> $GITHUB_OUTPUT
          echo "tag_name=v$latest" >> $GITHUB_OUTPUT
          
          # Pack format oku (jq olmadan)
          mcmeta="new_versions/$latest/datapack/pack.mcmeta"
          if [ -f "$mcmeta" ]; then
            pack_format=$(grep -oP '"pack_format"\s*:\s*\K\d+' "$mcmeta" || echo "1")
          else
            pack_format="1"
          fi
          
          echo "pack_format=$pack_format" >> $GITHUB_OUTPUT
          echo "Found version: $latest (pack format: $pack_format)"
      
      - name: Validate datapack structure
        run: |
          DATAPACK_DIR="new_versions/${{ steps.version.outputs.mc_version }}/datapack"
          
          if [ ! -d "$DATAPACK_DIR" ]; then
            echo "ERROR: Datapack folder not found: $DATAPACK_DIR"
            exit 1
          fi
          
          if [ ! -f "$DATAPACK_DIR/pack.mcmeta" ]; then
            echo "ERROR: pack.mcmeta not found"
            exit 1
          fi
          
          if [ ! -d "$DATAPACK_DIR/data" ]; then
            echo "ERROR: data folder not found"
            exit 1
          fi
          
          echo "✓ Datapack structure is valid"
          ls -R "$DATAPACK_DIR"
      
      - name: Create ZIP package
        run: |
          DATAPACK_DIR="new_versions/${{ steps.version.outputs.mc_version }}/datapack"
          ZIP_NAME="mc-server-toolkit-${{ steps.version.outputs.mc_version }}.zip"
          
          cd "$DATAPACK_DIR"
          zip -r "$GITHUB_WORKSPACE/$ZIP_NAME" . -x "*.git*" -x "*__pycache__*" -x "*.DS_Store"
          
          cd "$GITHUB_WORKSPACE"
          ls -lh "$ZIP_NAME"
          unzip -l "$ZIP_NAME"
      
      - name: Generate changelog
        id: changelog
        run: |
          PREV_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "")
          
          if [ -z "$PREV_TAG" ]; then
            echo "changelog=Initial release for Minecraft ${{ steps.version.outputs.mc_version }}" >> $GITHUB_OUTPUT
          else
            CHANGES=$(git log ${PREV_TAG}..HEAD --oneline --no-merges -- "new_versions/${{ steps.version.outputs.mc_version }}/datapack/**" | sed 's/^/- /' || echo "- No changes")
            echo "changelog<<EOF" >> $GITHUB_OUTPUT
            echo "$CHANGES" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
          fi
      
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.version.outputs.tag_name }}
          name: "MC Server Toolkit v${{ steps.version.outputs.mc_version }}"
          body: |
            ## 📦 Minecraft Server Toolkit Datapack
            
            **Minecraft Version:** ${{ steps.version.outputs.mc_version }}
            **Pack Format:** ${{ steps.version.outputs.pack_format }}
            
            ### 📝 Changelog
            ${{ steps.changelog.outputs.changelog }}
            
            ### 🚀 Installation
            1. Download the ZIP file below
            2. Place it in your world's `datapacks` folder
            3. Run `/reload` in-game
            
            ### 🐛 Issues
            [Report bugs](https://github.com/${{ github.repository }}/issues)
          files: mc-server-toolkit-${{ steps.version.outputs.mc_version }}.zip
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Summary
        if: success()
        run: |
          echo "## ✅ Release Created" >> $GITHUB_STEP_SUMMARY
          echo "**Version:** ${{ steps.version.outputs.mc_version }}" >> $GITHUB_STEP_SUMMARY
          echo "**Tag:** ${{ steps.version.outputs.tag_name }}" >> $GITHUB_STEP_SUMMARY
